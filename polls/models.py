from django.db import models


# Create your models here.
#class passwords(models.Model):
    #NameTag = models.CharField(max_length=64, required = True)
    #Password = models.CharField(max_length=64, required = True)
    #Description = models.TextField(default="Some description here ...", required = False)